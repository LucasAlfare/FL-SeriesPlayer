export function AppFooter() {
  return (
    <footer className="border-t border-white/10 bg-surface/70 px-4 py-4 text-sm text-textMuted backdrop-blur md:px-8">
      <div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
        <p>FL Series Player · desenvolvimento incremental em camadas</p>
        <p>Sem player, biblioteca funcional ou persistência visual nesta etapa.</p>
      </div>
    </footer>
  );
}
